% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_cube.R
\name{diagnose_cube}
\alias{diagnose_cube}
\title{Diagnose data quality of a processed data cube}
\usage{
diagnose_cube(data_cube, rules = "basic", verbose = TRUE, ...)
}
\arguments{
\item{data_cube}{A \code{processed_cube} object as returned by
\code{b3gbi::process_cube()}.}

\item{rules}{Diagnostic rules to evaluate. Can be:
\itemize{
\item A character vector referring to built-in rule sets
(e.g. \code{"basic"}, \code{"spatial"}).
\item A list of rule objects.
\item A combination of both.
}}

\item{verbose}{Logical indicating whether a diagnostic summary should be
printed.}

\item{...}{Additional arguments passed to \code{print.cube_diagnostics()} in case
\code{verbose = TRUE}.}
}
\value{
An object of class \code{cube_diagnostics}, containing one row
per metric with the following columns:
\itemize{
\item \code{dimension}: Dimension of the cube being evaluated
(e.g. \code{"spatial"}, \code{"temporal"}, \code{"taxonomical"}).
\item \code{metric}: Name of the diagnostic metric.
\item \code{value}: Computed metric value.
\item \code{severity}: Severity level (\code{"ok"}, \code{"note"}, \code{"important"},
\code{"very_important"}).
\item \code{message}: Human-readable description of the diagnostic result.
}

The rule objects are attached as an attribute of the diagnostics object.
}
\description{
Evaluates a set of diagnostic rules describing the data quality of a
biodiversity occurrence cube. Each rule computes a metric
on the cube and assigns a severity level indicating potential limitations
of the data for exploratory analysis or indicator calculation.
}
\examples{
# Example cube
# ! Real cubes should be processed with b3gbi::process_cube()
processed_cube <- list(
  data = data.frame(
    obs = c(5, 2, 10, 1),
    year = c(2001, 2001, 2002, 2003),
    minCoordinateUncertaintyInMeters = c(50, 2000, NA, 10)
  ),
  resolutions = "10km"
)
class(processed_cube) <- "processed_cube"

# Diagnose based on default rules
diag <- diagnose_cube(processed_cube)

# Sort diagnoses
diag <- diagnose_cube(processed_cube, sort_summary = "asc")

# Only show at least important diagnoses
diag <- diagnose_cube(processed_cube, filter_summary = "important")
}
\seealso{
Other data_exploration: 
\code{\link{filter_cube}()}
}
\concept{data_exploration}
