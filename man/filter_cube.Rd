% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_cube.R
\name{filter_cube}
\alias{filter_cube}
\title{Filter a processed data cube using diagnostic rules}
\usage{
filter_cube(data_cube, rules = NULL, diagnostics = NULL, ...)
}
\arguments{
\item{data_cube}{A \code{processed_cube} object.}

\item{rules}{Character vector or list of cube rule objects.
Ignored if \code{diagnostics} is supplied.}

\item{diagnostics}{Optional \code{cube_diagnostics} object returned by
\code{\link[=diagnose_cube]{diagnose_cube()}}. If provided, rules are extracted from this object.}

\item{...}{Additional arguments passed to rule-specific \code{filter_fn()}
functions.}
}
\value{
A filtered \code{processed_cube}.
}
\description{
Filters observations from a \code{processed_cube} based on rule definitions.
Filtering reuses the rule infrastructure used by \code{\link[=diagnose_cube]{diagnose_cube()}}, but
applies row-level filtering logic through rule-specific \code{filter_fn()}
functions.
}
\details{
The function evaluates rule-specific \code{filter_fn()} functions that return
a logical vector indicating which rows should be removed. Only rules that
implement a \code{filter_fn()} are applied. Rules without a filtering function
are ignored.

Filtering rules operate independently from diagnostic severity levels.
For example, a cube may have acceptable overall diagnostics while still
containing individual observations that fail filtering criteria.

After filtering, the function attempts to rebuild the cube using
\code{b3gbi::process_cube()} to ensure cube metadata remains consistent.
If this function is unavailable or fails, the filtered data replaces
\code{data_cube$data} directly and the original cube metadata is retained.
In that case a warning is issued.
}
\examples{
\dontrun{
# After processing a data cube with b3gbi::process_cube()

# Filter cube based on rule
filtered_cube1 <- filter_cube(
  processed_cube,
  rules = list(rule_spatial_miss_uncertainty())
)

# Filter cube based cube diagnostics
diag <- diagnose_cube(
  processed_cube,
  rules = list(
    rule_spatial_miss_uncertainty(),
    rule_temporal_min_years()
  )
)

filtered_cube2 <- filter_cube(
  processed_cube,
  diagnostics = diag
)

# The results are identical
identical(filtered_cube1$data, filtered_cube2$data)
}
}
\seealso{
Other data_exploration: 
\code{\link{cross_validate_cube}()},
\code{\link{diagnose_cube}()}
}
\concept{data_exploration}
