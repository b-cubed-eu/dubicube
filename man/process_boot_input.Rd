% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_boot_input.R
\name{process_boot_input}
\alias{process_boot_input}
\title{Normalize and dispatch bootstrap input}
\usage{
process_boot_input(
  bootstrap_results,
  grouping_var,
  type,
  conf,
  h,
  hinv,
  no_bias,
  boot_args
)
}
\arguments{
\item{bootstrap_results}{A dataframe with bootstrap replicates, a single
\code{boot} object, or a list of \code{boot} objects.}

\item{grouping_var}{Optional character vector of grouping variables used
to identify indicators or strata.}

\item{type}{Character vector specifying the type(s) of confidence interval
to compute (e.g. \code{"perc"}, \code{"bca"}, \code{"norm"}, \code{"basic"}).}

\item{conf}{Numeric confidence level between 0 and 1.}

\item{h}{Optional transformation function applied before CI calculation.}

\item{hinv}{Optional inverse transformation function.}

\item{no_bias}{Logical scalar. If \code{TRUE}, bias correction is skipped and
\code{boot} objects are converted to a dataframe. If \code{FALSE}, confidence
intervals are computed directly from the \code{boot} objects.}

\item{boot_args}{Optional list of additional arguments passed to
\code{boot::boot.ci()}.}
}
\value{
A list with two elements:
\itemize{
\item \code{data}: A dataframe of bootstrap replicates, or \code{NULL} if confidence
intervals were computed directly
\item \code{result}: A dataframe of confidence intervals, or \code{NULL} if the
dataframe workflow should continue.
}
}
\description{
Internal helper that standardizes bootstrap input supplied to
\code{\link[=calculate_bootstrap_ci]{calculate_bootstrap_ci()}} and handles early dispatch for \code{boot} objects.
It contains no statistical logic itself, but controls workflow routing.
}
\details{
The function supports three input modes:
\describe{
\item{Data frame input}{Returned unchanged and passed on to the
dataframe-based CI workflow.}
\item{\code{boot} input with \code{no_bias = TRUE}}{\code{boot} objects are converted
to a dataframe of bootstrap replicates and passed on to the dataframe-based
CI workflow.}
\item{\code{boot} input with \code{no_bias = FALSE}}{Confidence intervals are
computed directly using \code{boot::boot.ci()} logic and returned immediately.}
}
}
\seealso{
Other indicator_uncertainty_helper: 
\code{\link{boot_list_to_dataframe}()},
\code{\link{bootstrap_cube_raw}()},
\code{\link{calculate_boot_ci_from_boot}()},
\code{\link{derive_bootstrap_method}()},
\code{\link{resolve_bootstrap_method}()}
}
\concept{indicator_uncertainty_helper}
